const devConfig = {
    dbConfig: {
        host: "localhost",
        user: "postgres",
        password: "postgres",
        database: "postgres",
        port: 5432,
    },
    port: 8000
}

export { devConfig as config };
